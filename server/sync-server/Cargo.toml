[package]
name = "noderium-sync-server"
description = "Zero-knowledge E2E sync server (axum, Postgres, S3-compatible storage)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
# Source-available / proprietary (FSL/BUSL) — see ADR-009. Apache-2.0 client only.
license = "LicenseRef-FSL-1.1"
repository.workspace = true
authors.workspace = true

[[bin]]
name = "noderium-sync-server"
path = "src/main.rs"

[dependencies]
