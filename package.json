{
  "name": "noderium",
  "version": "0.0.1",
  "private": true,
  "description": "Local-first, lightweight, opinionated PKM tool (journal + Zettelkasten + SRS).",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "just dev",
    "build": "just build",
    "test": "just test",
    "lint": "just lint",
    "clean": "just clean",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp"]
  }
}
