//! Noderium desktop shell (Tauri 2) — stub.
//!
//! Phase 1 placeholder: compiles as a plain binary. The Tauri runtime, command
//! handlers, and the noderium-app-core plugin are wired in Phase 2 once the
//! SolidJS frontend (apps/desktop/frontend) exists to build a `dist/`.
fn main() {
    println!("noderium desktop shell (stub)");
}
