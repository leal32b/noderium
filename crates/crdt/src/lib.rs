//! noderium-crdt — Loro integration (ADR-002, ADR-003).
//! Tree (block hierarchy), Text (per-block content), Map (properties / SRS state).

// pub mod doc;          // Loro doc wrapper per note
// pub mod snapshot;     // snapshot + oplog encoding
// pub mod compaction;   // history compaction / shallow snapshots
// pub mod checkpoint;   // named checkpoints
