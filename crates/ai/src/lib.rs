//! noderium-ai — local embeddings in v1; generation pluggable and opt-in (ADR-007).

// pub mod embeddings;   // candle/fastembed ~384-dim model (bge-small/MiniLM)
// pub mod generation;   // trait for pluggable backend (Ollama / BYO-key)
