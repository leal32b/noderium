//! noderium-srs — FSRS scheduler (FR-6).
//! SRS state lives in the CRDT as properties; srs_cards is just a query index.

// pub mod fsrs;         // FSRS scheduling algorithm
// pub mod queue;        // review queue derivation
// pub mod card;         // note / cloze card model
