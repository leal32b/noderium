//! noderium-app-core — facade that orchestrates the domain crates and is
//! exposed to the desktop shell via Tauri commands/events and to mobile via FFI
//! (ADR-005, ADR-011).

// pub mod facade;       // high-level API surface
// pub mod commands;     // Tauri-command-friendly operations
// pub mod events;       // batched delta / index-update events
