[package]
name = "noderium-store"
description = "SQLite persistence: FTS5, migrations, and derived indexes (sqlite-vec planned)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
# `bundled` (set on the workspace dep) compiles SQLite with FTS5 enabled.
rusqlite.workspace = true
thiserror.workspace = true
